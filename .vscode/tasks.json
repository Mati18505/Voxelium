{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run tests",
      "type": "shell",
      "command": "cargo test --workspace --lib --bins --tests --benches"
    },
    {
      "label": "Run clippy",
      "type": "shell",
      "command": "cargo clippy --workspace --all-targets --all-features -- -D warnings"
    },
    {
      "label": "Check format",
      "type": "shell",
      "command": "cargo fmt --all -- --check"
    }
  ]
}